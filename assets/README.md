# Assets

First-party non-code inputs used by Styr development tools live here. Font source, generated binaries, preview headers and TTF inputs are grouped under `fonts/`. Runtime assets that are tightly coupled to the simulator remain beside the simulator source under `src/simulator/assets/`.
